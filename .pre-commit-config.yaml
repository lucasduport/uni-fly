repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        # mkdocs.yml uses !!python/name:... tags (PyYAML custom constructors)
        # for the Material emoji extension. The default check-yaml loader
        # refuses them; exclude rather than relax with --unsafe.
        exclude: ^mkdocs\.yml$
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=512]
      - id: check-merge-conflict
      - id: detect-private-key
      - id: mixed-line-ending
        args: [--fix=lf]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff-check
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format



  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks

  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.11.10
    hooks:
      - id: uv-lock

  # Local hook: run the test suite. Stage = pre-push so commits stay fast;
  # tests run once before the changes hit origin (and CI). Install with:
  #   uv run pre-commit install --hook-type pre-push
  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: uv run mypy packages/worker/src
        language: system
        files: ^packages/worker/src/
        pass_filenames: false
      - id: pytest
        name: pytest
        entry: uv run pytest
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
